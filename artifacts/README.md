# 构建产物与压缩包（Artifacts）

本目录用于存放**体积较大的 zip 等文件**（如队友工程整包、历史快照）。

- 默认已在 `.gitignore` 中忽略 `*.zip`，**避免将大文件推入 Git**。
- 需要共享时，请使用 **GitHub Releases**、网盘或对象存储，并在 Issue/文档中附链接。

若确需将某个 zip 纳入版本库，可在 `.gitignore` 中对该文件添加例外规则（不推荐）。
